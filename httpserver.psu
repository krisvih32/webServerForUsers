if POST request found:
    if first name empty:
        error out "first name required" bad request
    elif last name empty:
        error out "last name required" bad request
    if first name and last name in table of users:
        delete row of first name
        break
    add credentials to table
if GET request found:
    if first name not empty and last name not empty:
        error out everywhere first name and last name are in one row ok
    error out table ok
